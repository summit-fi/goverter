input:
    input.go: |
        package structs

        // goverter:converter
        type Converter interface {
            // goverter:map . Age | ExtendMethod
            Convert(source Input) Output
        }

        type Input struct {
            Name string
        }
        type Output struct {
            Name string
            Age int
        }
        func ExtendMethod(converter, source string) int {
            return 1
        }
error: |-
    error parsing 'goverter:map' at
        @workdir/input.go:6:5
        func (github.com/summit-fi/goverter/execution.Converter).Convert(source github.com/summit-fi/goverter/execution.Input) github.com/summit-fi/goverter/execution.Output

    error parsing type:
        func github.com/summit-fi/goverter/execution.ExtendMethod(converter string, source string) int

    first parameter must be of type github.com/summit-fi/goverter/execution.Converter but was string when having two parameters
