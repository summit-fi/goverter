input:
    input.go: |
        package structs

        // goverter:converter
        type Converter interface {
            // goverter:map . Score | Identity
            Convert(source Input) Output
        }

        type Input struct {
            Name string
        }
        type Output struct {
            Name string
            Score int
        }
        func Identity(source int) int {
            return source
        }
error: |-
    Error while creating converter method:
        func (github.com/summit-fi/goverter/execution.Converter).Convert(source github.com/summit-fi/goverter/execution.Input) github.com/summit-fi/goverter/execution.Output

    | github.com/summit-fi/goverter/execution.Input
    |
    |      | goverter:map . Score
    |      |
    source.
    target.Score
    |      |
    |      | int
    |
    | github.com/summit-fi/goverter/execution.Output

    Error using method:
        func github.com/summit-fi/goverter/execution.Identity(source int) int

    Method source type mismatches with conversion source: int != github.com/summit-fi/goverter/execution.Input
