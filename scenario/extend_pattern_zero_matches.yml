input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld.*
        type Converter interface {
        }
error: |-
    error parsing 'goverter:extend' at
        @workdir/input.go:5:1
        github.com/summit-fi/goverter/execution.Converter

    package github.com/summit-fi/goverter/execution does not have methods with names that match
    the golang regexp pattern "HelloWorld.*" and a convert signature
