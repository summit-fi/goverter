input:
    input.go: |
        package err

        // goverter:converter
        // goverter:name a b
        type Converter interface {
        }
error: |-
    error parsing 'goverter:name' at
        @workdir/input.go:5:1
        github.com/summit-fi/goverter/execution.Converter

    must have one value but got 2: "a b"
